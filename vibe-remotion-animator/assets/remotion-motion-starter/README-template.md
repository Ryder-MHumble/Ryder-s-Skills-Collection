# __PROJECT_NAME__

Generated by vibe-remotion-animator.

## Commands

```bash
npm install
npm run dev
npm run still -- --frame=90
npm run render
```

Edit `src/content.ts` for script and visual tokens. Edit `src/MotionCanvas.tsx` for choreography.
